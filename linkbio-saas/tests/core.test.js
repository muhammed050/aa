const test=require('node:test');const assert=require('node:assert/strict');test('username rules',()=>{for(const x of ['abc','user_1','creator-99'])assert.match(x,/^[a-z0-9_-]{3,30}$/);assert.doesNotMatch('A bad name',/^[a-z0-9_-]{3,30}$/)});test('supported templates',()=>assert.equal(new Set([1,2,3,4,5,6,7,8]).size,8));test('supported currencies',()=>assert.deepEqual(['USD','TRY','EUR','SYP'].sort(),['EUR','SYP','TRY','USD']));
