'use client';import {useEffect} from 'react';export default function AnalyticsTracker({pageId}:{pageId:string}){useEffect(()=>{const key=`rb-visitor-${pageId}`;let visitor=localStorage.getItem(key);if(!visitor){visitor=crypto.randomUUID();localStorage.setItem(key,visitor)}const session=sessionStorage.getItem('rb-session')||crypto.randomUUID();sessionStorage.setItem('rb-session',session);fetch('/api/events',{method:'POST',headers:{'content-type':'application/json'},keepalive:true,body:JSON.stringify({pageId,type:'page_view',sessionId:session,metadata:{visitorId:visitor,screenWidth:window.innerWidth}})});},[pageId]);return null}
