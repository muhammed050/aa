import type {MetadataRoute} from "next";import {createClient} from "@/lib/supabase/server";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const s=await createClient();const {data}=await s.from("pages").select("username,updated_at").eq("published",true).eq("noindex",false);return [{url:base,lastModified:new Date()},...(data||[]).map(p=>({url:`${base}/${p.username}`,lastModified:new Date(p.updated_at)}))]}
