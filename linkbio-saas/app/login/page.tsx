'use client';import {FormEvent,useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Login(){const [mode,setMode]=useState<'login'|'signup'>('login');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage('');const s=createClient();const r=mode==='login'?await s.auth.signInWithPassword({email,password}):await s.auth.signUp({email,password,options:{data:{name}}});setBusy(false);if(r.error)return setMessage(r.error.message);window.location.href='/dashboard'}return <main className='flex min-h-screen items-center justify-center bg-gray-50 p-4'><form onSubmit={submit} className='w-full max-w-md rounded-3xl border bg-white p-7 shadow-sm'><h1 className='text-3xl font-black'>رابطك</h1><p className='mt-2 text-gray-500'>{mode==='login'?'تسجيل الدخول إلى حسابك':'أنشئ صفحتك خلال دقائق'}</p>{mode==='signup'&&<input required value={name} onChange={e=>setName(e.target.value)} placeholder='الاسم' className='mt-6 w-full rounded-xl border p-3'/>}<input required type='email' value={email} onChange={e=>setEmail(e.target.value)} placeholder='البريد الإلكتروني' className='mt-3 w-full rounded-xl border p-3'/><input required minLength={8} type='password' value={password} onChange={e=>setPassword(e.target.value)} placeholder='كلمة المرور (8 أحرف على الأقل)' className='mt-3 w-full rounded-xl border p-3'/><button disabled={busy} className='mt-5 w-full rounded-xl bg-black p-3 font-bold text-white'>{busy?'جارٍ المعالجة…':mode==='login'?'دخول':'إنشاء الحساب'}</button>{message&&<p className='mt-3 text-sm text-red-600'>{message}</p>}<button type='button' onClick={()=>setMode(mode==='login'?'signup':'login')} className='mt-5 w-full text-sm underline'>{mode==='login'?'ليس لديك حساب؟ إنشاء حساب':'لديك حساب؟ تسجيل الدخول'}</button></form></main>}
