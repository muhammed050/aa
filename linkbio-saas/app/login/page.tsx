"use client";
import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {useRouter} from "next/navigation";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [signup,setSignup]=useState(false);const [msg,setMsg]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const s=createClient();const r=signup?await s.auth.signUp({email,password}):await s.auth.signInWithPassword({email,password});if(r.error){setMsg(r.error.message);return}router.push("/dashboard")}return <main className="container py-20 max-w-md"><div className="card p-8"><h1 className="text-3xl font-black">{signup?"إنشاء حساب":"تسجيل الدخول"}</h1><form onSubmit={submit} className="grid gap-4 mt-6"><input className="border rounded-xl p-3" type="email" placeholder="البريد الإلكتروني" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="border rounded-xl p-3" type="password" placeholder="كلمة المرور" value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required/><button className="btn btn-primary">{signup?"إنشاء حساب":"دخول"}</button></form>{msg&&<p className="text-red-600 mt-3">{msg}</p>}<button className="mt-5 underline" onClick={()=>setSignup(!signup)}>{signup?"لدي حساب بالفعل":"إنشاء حساب جديد"}</button></div></main>}
