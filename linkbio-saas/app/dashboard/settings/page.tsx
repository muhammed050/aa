import {createClient} from "@/lib/supabase/server";import {redirect} from "next/navigation";
export default async function Settings(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");return <main className="container py-8 max-w-2xl"><h1 className="text-3xl font-black">إعداد الصفحة</h1><div className="card p-6 mt-6"><form action="/api/page" method="post" className="grid gap-4"><input name="username" className="border rounded-xl p-3" placeholder="username" required minLength={3}/><input name="name" className="border rounded-xl p-3" placeholder="الاسم" required/><textarea name="bio" className="border rounded-xl p-3" placeholder="نبذة"/><select name="template" className="border rounded-xl p-3">{[1,2,3,4,5,6,7,8].map(x=><option key={x} value={x}>القالب {x}</option>)}</select><button className="btn btn-primary">حفظ</button></form></div></main>}
