import {redirect} from "next/navigation";import {currentUser} from "@/lib/data";
export default async function Billing(){if(!await currentUser())redirect("/login");return <main className="container py-8"><h1 className="text-3xl font-black">الفوترة</h1><div className="grid md:grid-cols-3 gap-4 mt-6">{[["Free","$0"],["Creator","$9"],["Business","$29"]].map(([n,p])=><div className="card p-6" key={n}><h2 className="text-xl font-black">{n}</h2><p className="text-3xl font-black mt-3">{p}<small>/شهر</small></p><button className="btn btn-primary mt-5 w-full" disabled={n==="Free"}>ترقية</button></div>)}</div><p className="muted mt-6">ضع WHOP_API_KEY وWHOP_WEBHOOK_SECRET وPlan IDs في بيئة Vercel لتفعيل الدفع.</p></main>}
