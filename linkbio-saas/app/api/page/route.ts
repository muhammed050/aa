import {NextResponse} from 'next/server';
import {z} from 'zod';
import {createClient} from '@/lib/supabase/server';
const section=z.object({id:z.string().min(1).max(80),type:z.enum(['profile','social','links','products','services','gallery','video','location','contact','whatsapp','booking','cta','text']),title:z.string().max(160).optional(),subtitle:z.string().max(300).optional(),visible:z.boolean(),content:z.record(z.string(),z.unknown()),style:z.record(z.string(),z.unknown()).optional()});
const body=z.object({sections:z.array(section).max(100),published:z.boolean().optional(),theme:z.record(z.string(),z.unknown()).optional(),name:z.string().min(1).max(120).optional(),bio:z.string().max(1000).nullable().optional(),template:z.number().int().min(1).max(8).optional(),seo_title:z.string().max(160).nullable().optional(),seo_description:z.string().max(320).nullable().optional(),noindex:z.boolean().optional()});
export async function PUT(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const parsed=body.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Invalid payload',details:parsed.error.flatten()},{status:400});const {data:page}=await s.from('pages').select('id').eq('user_id',user.id).maybeSingle();if(!page)return NextResponse.json({error:'Page not found'},{status:404});const update={...parsed.data,updated_at:new Date().toISOString()};const {data,error}=await s.from('pages').update(update).eq('id',page.id).select('*').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({page:data})}
export async function POST(req:Request){return PUT(req)}
