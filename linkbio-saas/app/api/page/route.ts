import {NextResponse} from 'next/server';import {z} from 'zod';import {createClient} from '@/lib/supabase/server';
const reserved=new Set(['admin','api','login','signup','dashboard','settings','billing','support','help','about','pricing','terms','privacy','null','undefined']);const username=z.string().transform(v=>v.trim().toLowerCase()).pipe(z.string().regex(/^[a-z0-9_-]{3,30}$/).refine(v=>!reserved.has(v),'اسم المستخدم محجوز'));const section=z.object({id:z.string().min(1).max(80),type:z.enum(['profile','social','links','products','services','gallery','video','location','contact','whatsapp','booking','cta','text']),title:z.string().max(160).optional(),subtitle:z.string().max(300).optional(),visible:z.boolean(),content:z.record(z.string(),z.unknown()),style:z.record(z.string(),z.unknown()).optional()});const body=z.object({username:username.optional(),sections:z.array(section).max(100),published:z.boolean().optional(),theme:z.record(z.string(),z.unknown()).optional(),name:z.string().min(1).max(120).optional(),bio:z.string().max(1000).nullable().optional(),template:z.number().int().min(1).max(8).optional(),seo_title:z.string().max(160).nullable().optional(),seo_description:z.string().max(320).nullable().optional(),noindex:z.boolean().optional()});async function auth(){const s=await createClient();const {data:{user}}=await s.auth.getUser();return {s,user}}export async function GET(){const {s,user}=await auth();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const {data:page}=await s.from('pages').select('*').eq('user_id',user.id).maybeSingle();return NextResponse.json({page})}export async function PUT(req:Request){const {s,user}=await auth();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const parsed=body.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'بيانات غير صالحة',details:parsed.error.flatten()},{status:400});let {data:page}=await s.from('pages').select('id').eq('user_id',user.id).maybeSingle();if(!page){const u=parsed.data.username||`user-${user.id.slice(0,8)}`;const {data:created,error}=await s.from('pages').insert({user_id:user.id,username:u,name:parsed.data.name||'صفحتي',sections:parsed.data.sections}).select('id').single();if(error)return NextResponse.json({error:error.code==='23505'?'اسم المستخدم مستخدم بالفعل':error.message},{status:400});page=created}const {data,error}=await s.from('pages').update({...parsed.data,updated_at:new Date().toISOString()}).eq('id',page.id).select('*').single();if(error)return NextResponse.json({error:error.code==='23505'?'اسم المستخدم مستخدم بالفعل':error.message},{status:400});return NextResponse.json({page:data})}export async function POST(req:Request){return PUT(req)}
